<%= form_for(current_user.relationships.find_by_followed_id(user),
             html: { method: :delete }) do |f| %>
   <button class="btn btn-default btn-block not-interested">
     <i class="icon ion-flask green"></i> Interested
   </button>
<% end %>