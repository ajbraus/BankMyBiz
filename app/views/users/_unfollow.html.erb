<%= form_for(current_user.relationships.find_by_followed_id(user),
             html: { method: :delete }) do |f| %>
   <button class="btn-sm btn btn-default not-interested">
     <i class="icon ion-person-add green"></i> Following
   </button>
<% end %>