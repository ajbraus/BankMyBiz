<div class="row">
  <h4 class='text-center'><%= @product.name %> Funders on Bankmybiz</h4>
  <%= render partial: 'matches/match', collection: @product.users.limit(4).order("RANDOM()"), as: 'match' %>
</div>