<%= form_for [post, post.comments.new] do |f| %>
  <div class="input-append">
    <%= f.text_field :content, class:'span4', id:"appendedInputButton" %>
    <button name="commit" type="submit" value="Create Comment" class="btn">
      <i class="icon-comment"></i>
    </button>
  </div>
<% end %>