<div class="tag-cloud text-left">
  <% if user_signed_in? %>
    <h4>Your Interests</h4>
<!--       <div class="input-group">
        <input type="text" class="form-control">
        <span class="input-group-btn">
          <button class="btn btn-success" type="button">Follow</button>
        </span>
      </div> -->
    <%= render partial: 'shared/tag', collection: current_user.tags.last(8) %>
  <% end %>
  <h4>Trending Interests</h4>
  <%= render partial: 'shared/tag', collection: Tag.order('use_count DESC').first(15).shuffle %>
</div>