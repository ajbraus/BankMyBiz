<%= render 'form' %>
<div class="row">
  <div class="span7 offset3">
<%= render partial: 'comments/comment', collection: @post.comments.where('content is not null').order('created_at asc'), as: :comment, locals: { post: @post } %>
  </div>
</div>