<tr>
  <td width='20%'><%= link_to "#{post.user.name}", user_path(post.user) %></td>
  <td class="muted" width="45%"><%= post.content %></td>
  <td width="20%">
  <%= raw post.tags.map(&:name).map { |t| link_to t, tag_path(t), class:"label" }.join(' ') %>
  </td>
</tr>