<%= form_for(current_user.relationships.build(followed_id: @user.id)) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "Follow #{@user.first_name_with_last_initial}", class: "btn btn-success" %>
<% end %>