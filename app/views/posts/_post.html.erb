<section class="newsfeed-item clearfix">
  <div class="row">
    <div class="col-xs-2">
      <%= profile_picture(post.user) %>
    </div>
    <div class="col-xs-8">
      <%= auto_link(post.content, html: { target: '_blank' }) %>
    </div>
    <div class="col-xs-2 grey large">
      <%= post.plusminus %>
      <% if user_signed_in? %>
        <% unless current_user == post.user%>
          <%= link_to vote_up_post_path(post), class:"breathing-room fade-in-green" do %>
            <i class="icon ion-plus-circled <%= user_signed_in? && current_user.voted_for?(post) ? 'green' : '' %>"  rel="tooltip" title="Give <%= post.user.first_name_with_last_initial %> a Buck"> </i>
          <% end %>
        <% end %>
      <% end %>
    </div>
  </div>
  <div class="row">
    <div class="text-right">
      <%= raw post.tags.map(&:name).map { |t| content_tag(:span, t, class:"tag small") }.join(' ') %>
    </div>
    <section class="text-right muted small">
      posted <%= time_ago_in_words(post.created_at) %> ago by 
      <% if user_signed_in? && current_user.bank == false %>
        <%= link_to post.user.first_name_with_last_initial, post.user if post.user %>
      <% else %>
        <%= post.user.first_name_with_last_initial if post.user %>
      <% end %>
    </section>
    <% if user_signed_in? && (post.user == current_user || current_user.admin?) %>
      <div class="text-right smaller muted">
        <%= link_to "Edit Post", edit_post_path(post) %> |
        <%= link_to 'Delete Post', post, method: :delete, data: { confirm: 'Are you sure you would like to delete this post?' } %>
      </div>
    <% end %>
    <section class='text-right'>
      <a href="mailto:?subject=Interesting Post&body=Here is a post from Bankmybiz.com I thought you'd like%0D%0A%0D%0A<%= post.content %>%0D%0A%0D%0ABy: <%= post.user.first_name_with_last_initial %>" class="btn btn-default btn-xs btn-rounded"><i class="icon ion-ios7-email-outline"></i> email</a>
      <a href="https://twitter.com/share?url=https%3A%2F%2Fwww.bankmybiz.com%2Fposts%2F<%= post.id %>&text=<%= post.content %>" target='_blank'>
        <span class="btn btn-xs btn-rounded btn-twitter"><i class="icon ion-social-twitter"></i> twitter
      </a>
      <a href="#" 
        onclick="
          window.open(
            'https://www.facebook.com/sharer/sharer.php?u='+encodeURIComponent(location.href), 
            'facebook-share-dialog', 
            'width=626,height=436'); 
          return false;" class="no-text-decoration">
        <span class="btn btn-xs btn-rounded btn-facebook"><i class="icon ion-social-facebook"></i> facebook</span>
      </a>
      <a href="http://www.linkedin.com/shareArticle?mini=true&url=https%3A%2F%2Fwww.bankmybiz.com%2Fposts%2F<%= post.id %>&title=<%= post.content %>&source=Bankmybiz.com" rel="nofollow" onclick="NewWindow(this.href,'template_window','550','400','yes','center');return false" onfocus="this.blur()" target="_blank">
        <span class="btn btn-xs btn-rounded btn-linkedin"><i class="icon ion-social-linkedin"> linkedin</i>
      </a>
    </section>
  </div>
  <%= render partial: 'comments/form', locals: { post: post } %>
  <%= render partial: 'comments/comment', collection: post.comments.where('content is not null').order('created_at desc'), as: :comment, locals: { post: post } %>
  
</section>
<div class="hr-line"></div>